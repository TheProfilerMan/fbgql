{
    "actorOutputSchemaVersion": 1,
    "title": "Facebook Comments Scraper output",
    "description": "One item per scraped post, each with its comments and replies, stored in the default dataset.",
    "properties": {
        "comments": {
            "type": "string",
            "title": "Scraped comments",
            "description": "All scraped posts with their comments and replies, as dataset items.",
            "template": "{{links.apiDefaultDatasetUrl}}/items"
        }
    }
}
